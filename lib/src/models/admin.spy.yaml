class: Admin
table: admins
fields:
  email: String
  passwordHash: String
  name: String?
  createdAt: DateTime
  isActive: bool, default=true
indexes:
  admin_email_unique_idx:
    fields: email
    unique: true